# .bashrc

export PYENV_ROOT="$HOME/.pyenv"
command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"

alias normpy='pycodestyle --show-source --show-pep8'
alias pyfmt='find . -name '\''*.py'\'' -exec autopep8 --in-place --aggressive --experimental '\''{}'\'' \;'

